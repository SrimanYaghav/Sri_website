Put your exported Blender models here as .glb files
(e.g. model1.glb, model2.glb) to match the src paths
used in 3d-animation.html.

In Blender: File > Export > glTF 2.0 (.glb/.gltf)
Choose the .glb (binary) format for a single-file export.
